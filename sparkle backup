window.addEventListener('mousemove', (e) => {
    // Increase this number for a "heavier" sparkle trail
    for (let i = 0; i < 4; i++) {
        beams.push(new FluentBeam(e.clientX, e.clientY));
    }
});
